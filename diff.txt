--- copy
+++ current
@@ -13,7 +13,7 @@
 from dotenv import load_dotenv
 
 from ..config import (
-    MODEL_PRO, MODEL_FLASH, TOP_K_WHY_IT_MATTERS,
+    MODEL_PRO, MODEL_PRO_ULTRA, MODEL_FLASH, MODEL_GROK, TOP_K_WHY_IT_MATTERS,
     CATEGORY_MAP, SERVICE_MAP, CORE_MAP, get_allowed_tags
 )
 
@@ -29,6 +29,7 @@
     
     def __init__(self):
         self.pro_model = genai.GenerativeModel(MODEL_PRO)
+        self.ultra_model = genai.GenerativeModel(MODEL_PRO_ULTRA)
         self.flash_model = genai.GenerativeModel(MODEL_FLASH)
         self.top_k = TOP_K_WHY_IT_MATTERS
         self.allowed_tags = get_allowed_tags()
@@ -56,29 +57,29 @@
     def _create_scoring_prompt(self, article: dict) -> str:
         """Create prompt for impact scoring."""
         return f"""
-You are an expert AI news analyst. Analyze this article and calculate its impact score.
+You are an expert AI news analyst. Evaluate this article strictly on a 1 to 5 scale.
+1 = Garbage/Spam/Irrelevant/Routine update
+2 = Minor news, slight feature updates
+3 = Standard news, moderate interest to practitioners
+4 = Important news, high impact on the AI field
+5 = Must-read, massive breakthrough or industry-shifting event
 
 Title: {article.get('raw_title', '')}
 Content: {str(article.get('raw_content', ''))[:3000]}
 Source: {article.get('source_name', '')}
 
-Calculate the impact score based on these criteria (each 0-25):
-- industry: Industry-wide impact (disruption, market changes)
-- tech: Technical significance (innovation, breakthrough)
-- reach: Audience reach and public interest
-- longTerm: Long-term implications
-- socialEthics: Social/ethical considerations
-
-Output JSON:
+Evaluate based on:
+- Industry-wide impact
+- Technical significance
+- Audience reach
+- Long-term implications
+- CRITICAL BONUS: Give a HIGH SCORE to news dealing with large-scale adoption by general users (e.g. App Store ranking entry, MAU surge, etc.) as this is very important for AI popularization.
+- CRITICAL BONUS: Give a HIGH SCORE to news about new AI technologies from Claude, OpenAI, Gemini, Grok, or announcements of highly performant open-source models.
+
+Output JSON ONLY:
 {{
-  "impactScore": <total 0-100>,
-  "impactDetails": {{
-    "industry": <0-25>,
-    "tech": <0-25>,
-    "reach": <0-25>,
-    "longTerm": <0-25>,
-    "socialEthics": <0-25>
-  }}
+  "impactScore": <1-5 integer>,
+  "reasoning": "<short sentence justifying the score>"
 }}
 """
     
@@ -163,7 +164,7 @@
                         {"role": "system", "content": "You are an expert AI news analyst. Respond only with JSON."},
                         {"role": "user", "content": prompt}
                     ],
-                    "model": "grok-4-1-fast-reasoning",
+                    "model": MODEL_GROK,
                     "stream": False,
                     "temperature": 0.1
                 },
@@ -198,57 +199,71 @@
         
         results = []
         
-        # Step 1: Score all articles using Pro model
-        print(f"[ScorerTagger] Scoring {len(articles)} articles with {MODEL_PRO}...")
+        # Step 1: Score all articles using Pro model + Tournament Style
+        print(f"[ScorerTagger] Scoring {len(articles)} articles with {MODEL_PRO} (Tournament-style 1-5 scale)...")
         
         for i, article in enumerate(articles):
             print(f"  [{i+1}/{len(articles)}] Scoring: {article.get('raw_title', '')[:40]}...")
             try:
-                # 1. Gemini Scoring
+                # Build prompt
                 prompt = self._create_scoring_prompt(article)
+                scores = []
+                
+                # Round 1: Gemini Pro
                 score_data = self._generate_with_retry(self.pro_model, prompt)
-                
-                gemini_score = score_data.get('impactScore', 0)
-                
-                # Exposure Bonus (applied to gemini_score first)
+                gemini_score = float(score_data.get('impactScore', 1))
+                scores.append(gemini_score)
+                print(f"    [Round 1] Gemini Pro Score: {gemini_score}/5 ({score_data.get('reasoning', 'No reasoning')})")
+                
+                # Round 2: Grok Refinement (Only if Gemini score >= 3)
+                if gemini_score >= 3.0:
+                    print(f"    [Round 2] Promising article ({gemini_score}/5). Evaluating with Grok...")
+                    grok_score = self._score_with_grok(article)
+                    if grok_score > 0:
+                        scores.append(grok_score)
+                        print(f"    [Round 2] Grok Score: {grok_score}/5")
+                        time.sleep(2)
+                        
+                        # Round 3: Final Gemini Verification (Only if Current Average >= 4)
+                        avg_current = sum(scores) / len(scores)
+                        if avg_current >= 4.0:
+                            print(f"    [Round 3] Top Tier Candidate (Avg {avg_current:.1f}/5). Final Gemini 3.1 Pro Verification...")
+                            final_score_data = self._generate_with_retry(self.ultra_model, prompt)
+                            final_gemini = float(final_score_data.get('impactScore', 1))
+                            scores.append(final_gemini)
+                            print(f"    [Round 3] Final Verification Score: {final_gemini}/5")
+                
+                # Calculate True Average (1-5 scale)
+                avg_star = sum(scores) / len(scores)
+                
+                # Convert to 100 point scale (1 -> 20, 5 -> 100)
+                base_100_score = avg_star * 20
+                
+                # Exposure Bonus (+2 pts per extra mention)
                 exposure = article.get('exposure_score', 0)
-                gemini_score = min(100, gemini_score + exposure * 2)
+                final_100_score = min(100.0, base_100_score + exposure * 2)
                 
                 article['gemini_score'] = gemini_score
-                article['impactDetails'] = score_data.get('impactDetails', {})
-                article['impactDetails']['exposure_score'] = exposure
-                
-                # 2. Multi-Model Refinement (Grok)
-                final_score = gemini_score
-                grok_score_1 = 0
-                grok_score_2 = 0
-                
-                if gemini_score >= 85:
-                    print(f"    [+] High Impact ({gemini_score}). Refining with Grok...")
-                    grok_score_1 = self._score_with_grok(article)
-                    time.sleep(5)
-                    grok_score_2 = self._score_with_grok(article)
-                    
-                    if grok_score_1 > 0 and grok_score_2 > 0:
-                        avg_score = (gemini_score + grok_score_1 + grok_score_2) / 3
-                        final_score = avg_score
-                        print(f"    [=] Refined Score: {final_score:.1f} (Gemini: {gemini_score}, Grok: {grok_score_1}, {grok_score_2})")
-                    else:
-                        print(f"    [!] Grok scoring failed. Reverting to Gemini score.")
-                
-                article['grok_score_1'] = grok_score_1
-                article['grok_score_2'] = grok_score_2
-                article['impactScore'] = final_score
-                
+                article['grok_score_1'] = scores[1] if len(scores) > 1 else 0
+                article['grok_score_2'] = scores[2] if len(scores) > 2 else 0
+                article['impactScore'] = final_100_score
+                article['impactDetails'] = {
+                    "avg_star": avg_star, 
+                    "rounds": len(scores),
+                    "exposure_bonus": exposure * 2,
+                    "reasoning": score_data.get('reasoning', '')
+                }
+                
+                print(f"    [=] Final Score: {final_100_score:.1f}/100 (Avg Star: {avg_star:.2f}, Rounds: {len(scores)}, Bonus: +{exposure*2})")
                 time.sleep(2)
                 
             except Exception as e:
                 print(f"    ❌ Error scoring: {e}")
-                article['gemini_score'] = 0
+                article['gemini_score'] = 1
                 article['grok_score_1'] = 0
                 article['grok_score_2'] = 0
-                article['impactScore'] = 0
-                article['impactDetails'] = {"industry": 0, "tech": 0, "reach": 0, "longTerm": 0, "socialEthics": 0}
+                article['impactScore'] = 20 # Minimum score
+                article['impactDetails'] = {"avg_star": 1, "rounds": 1, "exposure_bonus": 0, "error": str(e)}
         
         # Sort by impact score
         articles.sort(key=lambda x: x.get('impactScore', 0), reverse=True)
